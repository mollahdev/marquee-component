export const MARQUEE_COMPONENT = 'marquee-component';
export const MARQUEE_MOVE = 'marquee-move';
export const MARQUEE_SLIDE = 'marquee-slide';
export const EVENT_NAME = 'marquee-mount'

export const MODE = Object.freeze({
    HORIZONTAL: 'horizontal',
    VERTICAL: 'vertical'
})

export const DIRECTION = Object.freeze({
    LTR: 'ltr',
    RTL: 'rtl'
});